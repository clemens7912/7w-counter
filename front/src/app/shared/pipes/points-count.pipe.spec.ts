import { PointsCountPipe } from './points-count.pipe';

describe('PointsCountPipe', () => {
  it('create an instance', () => {
    const pipe = new PointsCountPipe();
    expect(pipe).toBeTruthy();
  });
});
